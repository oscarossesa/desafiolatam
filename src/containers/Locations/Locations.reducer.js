const initialState = {
  collection: []
}

function locationsReducer (state = initialState, action) {
  switch (action.type) {
    default: {
      return state
    }
  }
}

export default locationsReducer